# Worse Engine

An experimental game engine for exploring rendering engine architecture, graphics. Built with modern C++, currently focused on rendering capabilities.

!!! REFACTORING WIP !!!


# Build project

**Scripts/utt.sh**: Tool script to generate project, clean, etc.

```sh
./Scripts/utt.sh generate default

cmake --build Worse/Build/default
```